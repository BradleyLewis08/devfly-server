{
  "name": "devfly-server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "apollo-server": "^3.10.0",
    "graphql": "^16.5.0",
    "mongoose": "^6.5.0",
    "nexus": "^1.3.0",
    "ts-node-dev": "^2.0.0"
  },
  "scripts": {
    "dev": "ts-node-dev -r dotenv/config --transpile-only --exit-child --no-notify src/index.ts",
    "generate": "ts-node --transpile-only src/schema.ts"
  },
  "devDependencies": {
    "dotenv": "^16.0.1",
    "typescript": "^4.7.4"
  }
}
